<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error - Let'sDraw</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <style>
    .error-container {
      text-align: center;
      margin-top: 100px;
      padding: 20px;
    }
    .error-code {
      font-size: 4rem;
      color: #e74c3c;
      margin-bottom: 20px;
    }
    .error-message {
      font-size: 1.5rem;
      color: #333;
      margin-bottom: 30px;
    }
    .back-button {
      display: inline-block;
      padding: 10px 20px;
      background-color: #3498db;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      transition: background-color 0.3s;
    }
    .back-button:hover {
      background-color: #2980b9;
    }
  </style>
</head>
<body>
  <div class="error-container">
    <div class="error-code">
      <%= typeof status !== 'undefined' ? status : '500' %>
    </div>
    <div class="error-message">
      <%= message || 'An unexpected error has occurred' %>
    </div>
    <% if (process.env.NODE_ENV === 'development' && error && error.stack) { %>
      <pre style="text-align: left; background: #f4f4f4; padding: 20px; margin: 20px; border-radius: 5px;">
        <%= error.stack %>
      </pre>
    <% } %>
    <a href="/" class="back-button">Back to Home</a>
  </div>
</body>
</html>